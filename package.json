{
  "name": "isaaccompanion",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "rebuild:data": "node scripts/rebuild-data.mjs",
    "icons": "node scripts/download-icons.mjs",
    "icons:force": "node scripts/download-icons.mjs --force",
    "build:fallback": "node scripts/build-fallback.js",
    "data": "npm run rebuild:data && npm run icons && npm run build:fallback && npm run check:data",
    "check:data": "node scripts/check-data.js",
    "lint": "eslint . --ext .js,.mjs",
    "format": "prettier --check .",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^9.29.0",
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8"
  }
}
